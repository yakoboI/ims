# NPM Configuration for Railway
# Retry configuration for npm registry issues
fetch-retries=10
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
fetch-timeout=300000

# Use npm registry with fallback
registry=https://registry.npmjs.org/

# Alternative: Use Cloudflare mirror if main registry fails
# registry=https://registry.npmmirror.com/

# Log level - use warn to see retry attempts
loglevel=warn

# Network settings
maxsockets=15

